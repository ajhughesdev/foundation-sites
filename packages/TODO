// TODO: Extract Reveal into its own package (packages/reveal) and keep @foundation/core runtime-only.
// DONE: Add Playwright + axe smoke tests for Reveal, Dropdown, Tooltip, Tabs, Accordion (a11y + keyboard regressions).
// DONE: Wire `yarn f7:test` into CI as a separate job.
// DONE: Add focus trap for non-<dialog> modals (Reveal fallback).
// TODO: Consider inerting page content behind non-<dialog> modals (optional a11y improvement).
// DONE: Accordion component (simple + a11y)
// DONE: OffCanvas component (draft + example + tests)
// DONE: Dev workflow: `yarn f7:dev` runs tsc watch + Vite for hot reload.
// DONE: Toast/Notification component (draft + example + tests)
// TODO: CSS expansion: start core layout primitives (grid/flex helpers), buttons/forms, and a more complete reset/base layer.
// TODO: Add Playwright caching/parallel tweaks for faster CI.
// TODO: Focus-trapping/inert for non-<dialog> modals (extract a small focusTrap util and apply it to the Reveal fallback + tighten Playwright coverage).
// TODO: Start the CSS expansion: baseline button/input styles + a couple layout primitives (container/stack/grid) so the examples stop hand-rolling UI scaffolding.
// TODO: After the component APIs feel stable, extract Reveal (and later others) into their own packages (packages/reveal, etc.) to keep @foundation/core runtime-only.
