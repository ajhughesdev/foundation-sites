// DONE: Extract Reveal into its own package (packages/reveal) and keep @foundation/core runtime-only.
// DONE: Extract Dropdown into its own package (packages/dropdown).
// DONE: Extract Tooltip into its own package (packages/tooltip).
// DONE: Extract Tabs into its own package (packages/tabs).
// DONE: Extract Accordion into its own package (packages/accordion).
// DONE: Extract OffCanvas into its own package (packages/offcanvas).
// DONE: Extract Toast into its own package (packages/toast).
// DONE: Add Playwright + axe smoke tests for Reveal, Dropdown, Tooltip, Tabs, Accordion (a11y + keyboard regressions).
// DONE: Wire `yarn f7:test` into CI as a separate job.
// DONE: Add focus trap for non-<dialog> modals (Reveal fallback).
// DONE: Inert page content behind non-<dialog> modals (Reveal fallback + OffCanvas).
// DONE: Accordion component (simple + a11y)
// DONE: OffCanvas component (draft + example + tests)
// DONE: Dev workflow: `yarn f7:dev` runs tsc watch + Vite for hot reload.
// DONE: Toast/Notification component (draft + example + tests)
// DONE: CSS expansion: initial layout utilities + button/form defaults + richer base typography.
// DONE: Add Playwright caching/parallel tweaks for faster CI.
// DONE: Focus-trapping/inert for non-<dialog> modals (extract focusTrap/inert utils + apply to Reveal fallback + Playwright coverage).
// DONE: Start the CSS expansion: baseline button/input styles + layout primitives (container/stack/grid) so the examples stop hand-rolling UI scaffolding.

// NEXT (publishability + release hygiene):
// TODO: Decide long-term package linking strategy (keep `yarn f7:link` vs. introduce workspaces in a future repo/branch).
// TODO: Add a simple multi-package version/publish helper (optional; keep manual publish in `FOUNDATION7-PUBLISHING.md` until needed).
// TODO: Tighten semver ranges + document API stability expectations per package once we cut the first public alpha.
